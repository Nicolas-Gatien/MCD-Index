document.addEventListener('DOMContentLoaded', (event) => {
    const uploadSubmit = document.getElementsByName('upload')
    const uploadFile = document.getElementsByName('file')

    console.log(uploadSubmit)
    console.log(uploadFile)
});